init_config:
  instances:
    - host: ${$(echo $DATABASE_URL | cut -d '@' -f 2 | cut -d ':' -f 1)}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
      dbm: true
      port: ${DB_PORT}
      dbname: ${DB_NAME}
      ssl: required
      collect_schemas:
        enabled: true