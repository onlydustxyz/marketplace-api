infrastructure:
  github:
    personal-access-token: "GITHUB_PAT"
  od:
    api:
      client:
        api-key: "some-rust-api-key"
  indexer:
    api:
      client:
        api-key: "some-indexer-api-key"
spring:
  main:
    allow-bean-definition-overriding: true
# Uncomment lines below if you want to see Hibernate SQL queries in the console
#  jpa:
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#logging:
#  level:
#    org:
#      hibernate:
#        type: trace